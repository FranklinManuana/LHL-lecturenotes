In the previous activities, we used HTTP requests to some extent but, since then, we have learned what they are and how they work. With that knowledge in hand, we can now go deeper into the library and get even more familiar with the topic through practice.

Instruction

Follow [the tutorial **"Python’s Requests Library (Guide)"** by Real Python](https://realpython.com/python-requests/). Since we are using the `Anaconda` distribution of Python, we don't have to install the library. So let's jump directly to the section called **"The GET Request"**.