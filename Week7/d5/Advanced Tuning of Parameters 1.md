This challenge will test a combinations of skills we've acquired in the preceding sections. We will:

- Build a pipeline to tune parameters with Grid Search.
- Combine different pipelines with FeatureUnion.
- Create our own classes inside the pipeline.

## Challenge

We will use the same data we are already familiar with from previous activities, Prediction of Sales.

Instruction

Use the same [**repository**](https://github.com/lighthouse-labs/pipelines_and_persistence_exercise.git) we cloned earlier and follow the instructions in the `pipelines_challenge.ipynb` notebook.